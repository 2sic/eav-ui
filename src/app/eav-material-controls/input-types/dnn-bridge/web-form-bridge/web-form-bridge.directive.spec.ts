import { WebFormBridgeDirective } from './web-form-bridge.directive';

describe('WebFormBridgeDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new WebFormBridgeDirective();
  //   expect(directive).toBeTruthy();
  // });
});
