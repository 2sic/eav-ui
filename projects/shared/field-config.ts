export interface FieldConfig {
  /**
   * spm 2019.03.29 write JSDoc for every property
   */
  disabled?: boolean;
  label?: string;
  name: string;
  placeholder?: string;
  inputType: string;
  type?: string;
  required?: boolean;
  index?: number;
}
