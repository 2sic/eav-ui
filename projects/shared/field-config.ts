export interface FieldConfig {
  /**
   * spm 2019.03.29 write JSDoc for every property
   */
  name: string;
  index: number;
  label: string;
  placeholder: string;
  inputType: string;
  type: string;
  required: boolean;
  disabled: boolean;
}
